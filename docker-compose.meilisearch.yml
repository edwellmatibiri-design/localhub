services:
  meilisearch:
    image: getmeili/meilisearch:v1.22
    container_name: localhub-meili
    environment:
      MEILI_MASTER_KEY: ${MEILISEARCH_API_KEY:-masterKey}
      MEILI_ENV: development
    ports:
      - "7700:7700"
    volumes:
      - ./.meili_data:/meili_data
