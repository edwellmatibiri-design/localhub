export default function SeoCategorySuburbPage({ category, suburb }: { category: string; suburb: string }) {
  return <div className="card">AI SEO combo page template for <strong>{category}</strong> in <strong>{suburb}</strong>.</div>;
}
